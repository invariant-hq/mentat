; Unit suite for the executable-side charter store (bin/boot). Its source
; compiles against [Fs] and [User_dirs], whose flat-directory copies are
; already owned by the test_fs and test_user_dirs stanzas one level up, so
; this suite lives in its own directory with its own copies.

(copy_files
 (files ../../../bin/boot/charter_store.{ml,mli}))

(copy_files
 (files ../../../bin/boot/fs.{ml,mli}))

(copy_files
 (files ../../../bin/boot/user_dirs.{ml,mli}))

(test
 (name test_charter_store)
 (modules test_charter_store charter_store fs user_dirs)
 (libraries
  windtrap
  mentat_charter
  mentat_digest
  mirage-crypto-rng
  mirage-crypto-rng.unix
  eio
  eio.unix
  eio_main
  unix
  jsont
  jsont.bytesrw))
